// TEMP STUB for sandbox build verification only (no network access to
// Google Fonts here). Real fonts.ts is restored after this check.
export const sourceSerif = { variable: "--font-source-serif" };
export const plexSans = { variable: "--font-plex-sans" };
export const plexMono = { variable: "--font-plex-mono" };
